[package]
name = "alpha-rerank-server"
version = "0.1.0"
edition = "2024"

[dependencies]
alpha-input = { path = "../third_party/alpha-input" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tiny_http = "0.12"
